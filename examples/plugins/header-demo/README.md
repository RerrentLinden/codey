# Codey 请求头示例插件

独立的原生插件，演示请求头扩展、配置读取和插件数据持久化。

- 启用后为路由请求添加 `X-Plugin-Demo`，默认值为 `hello-codey`。
- 点击配置可查看字段说明并编辑配置文件，修改 `value` 后重新启用插件生效；只改 `_comments` 说明无需重新启用。
- 支持检查当前配置、保存和读取示例数据，升级与重新启用保留数据。

导入后默认停用。仅加载可信来源的原生插件，它拥有宿主进程权限。开发接口与打包方式见仓库的 Codey Plugin SDK 文档。
